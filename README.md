# music-generator-ai
An AI-powered Music Generation system built using Python, MIDI datasets, and deep learning models like LSTM/RNN. The project preprocesses musical note sequences, trains a neural network to learn music patterns, and generates new melodies that can be converted into MIDI or audio files.
